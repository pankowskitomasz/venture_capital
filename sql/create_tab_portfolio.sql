CREATE TABLE venturecapital_portfolio(
    id INT NOT NULL AUTO_INCREMENT,
    title VARCHAR(80) NOT NULL,
    category VARCHAR(80) NOT NULL,
    description VARCHAR(250) NOT NULL,
    PRIMARY KEY(id)
);